# v1.2.2 前置核实报告 — 渲染异常与双向滚动抖动

> 核实时间：2026-09-09 晚 · 基线：v1.2.1（`d985486`，Release 已发布）
> 用户反馈：① 第 6/9 章渲染异常（4 张截图）；② 菜单 3+4+5 同开时编辑器滚动抖动、滚不到底，预览跟随抖动

---

## 1. 渲染问题核实结果（✅ 已闭环：非代码 bug，是 `---` 分隔线的 CommonMark 语义）

用户提供样本 `k4refresh 测试策略修订版20260909.md` 后全文件实测（wasm = 发布版同一渲染核心），**渲染核心输出完全正确**（代码块逐字保留、`##` 全部为 h2）。真相：

> 文档中多处 **`---` 分隔线紧贴段落文字（前无空行）**，按 CommonMark 规范（GitHub 同样），`---` 会把紧邻的上一段"下划线"成 **setext 二级标题**——整段变为 24px 大标题。

全文件共 3 处受影响（实测清单）：`@114 interval 决策规则整段`、`@128 KOReader 段落`、`@172 bench_base.csv 段落`——与用户两张截图完全对应（第 6 章底部大字段落、第 9 章大字段落）。代码块（`@107-113`）逐字保留无任何"列表化"。

**处置**：
1. 已生成修正副本 `k4refresh 测试策略修订版20260909-fixed.md`（为分隔线 `---` 前补空行；复渲染验证 3 处全部还原为 `<hr>`，代码块与标题不受影响）——**请用副本替换原文件或按此规则修改原文**；
2. README 新增 FAQ 说明该语义；
3. 渲染核心保持规范行为，**不做改动**。

### 1.1 排查证据链（初判 → 样本闭环）

初判阶段（仅凭截图重建源码）曾怀疑渲染管线，逐一排除后均正确：

| 环节 | 核查方式 | 结果 |
|------|----------|------|
| comrak 管线 | wasm 实测（= 发布版同一核心）4 种重建形态 + `lib.rs` 静态审 | 单次解析、无二次解析，围栏/标题输出正确 ✅ |
| style.css | 静态审查 | `h2=24px`、`h4=16px`、`pre/code` 等宽 ✅ |
| outline / mermaid / checkbox / radio 注入 JS | 逐行审 | 均不改写正文结构 ✅ |
| 渲染缓存 | `RenderCacheKey` 含全文 | 按内容寻址，无陈旧命中 ✅ |

正因"截图现象无法由截图源码产生"，才请求样本文件；拿到 `k4refresh 测试策略修订版20260909.md` 后全文件实测立即破案——**文档中 `---` 分隔线紧贴段落（前无空行），按 CommonMark 规范（GitHub 同样）成为上一段的 setext 二级标题**。截图截掉了行间空行状态，且 editor 截图行距紧凑，肉眼无法分辨"有无空行"，导致初判与现象看似矛盾。

### 1.2 最终结论与处置

见本节开头。三处受影响行号实测：114 / 128 / 172（另 125-126 处 `---` 紧贴段落但因前文是列表语境未成标题——规范行为）。**渲染核心零改动**。

---

## 2. 双向滚动抖动核实结果（根因确认，已修复）

用户场景 = 菜单 3（first-visible-line 前向同步）+ 4（预览→编辑器反向同步）+ 5（outline）同开。菜单槽位本身无 bug（2=光标同步、3=fvl 同步、4=反向、5=outline，代码与注册顺序一致）。抖动由四个机制叠加：

### 根因 1（主因）：前向驱动无"程序性滚动抑制"

`ScrollToElementWithLineNo`（编辑器→预览）执行 `window.scrollTo` 后，预览注入的 `scroll` 监听器（W4，rAF 节流）会把这个**我们自己驱动的滚动**当作用户滚动，上报 `previewScroll;N` → 反向回写编辑器 → 编辑器又触发前向……**反馈环**。W4 让上报从 v1.2 的"仅 scrollend"变为"滚动过程中持续"，把环从偶发变成实时，这是 v1.2.1 抖动明显化的直接原因。

### 根因 2：W5 的 300ms 前向静默窗造成"死-突发"交替

反向每次回写后 300ms 内前向被静默：用户持续滚动编辑器时，预览先"死"300ms 再突然跳一段，循环往复 = **上下抖动感**。

### 根因 3：反向回写把编辑器量化到"块起点"，滚不到底

`OnPreviewScrolled` 用块 `data-line`（块起始源码行）`SetFirstVisibleLine`。长块的块起点远在视口上方：编辑器滚近底部时被反复拽回块起点，**永远无法顺利到底**。

### 根因 4（次要）：前向查询差一行

前向用 0 基 doc line 查询 1 基的 `data-line`（`OnPreviewScrolled` 已做 -1 换算，前向没有做 +1），依赖"≤ 回退"兜底，加重落点偏差。

### 修复方案（F1–F4）

| # | 修复 | 位置 | 内容 |
|---|------|------|------|
| F1 | 程序性滚动抑制（方向锁） | `Webview2WebbrowserControl.cs` 注入 JS | `scrollScript` 执行前打标 `__mdProgScroll`（时间戳）+ `__mdProgTarget`（目标块 data-line）；`report()` 在 600ms 窗口内对目标块的回落上报直接丢弃；目标块之外的行（用户已在滚动）照常上报 |
| F2 | 移除 300ms 静默窗 | `MarkdownPanelController.cs` | F1 已断环，静默窗只剩副作用；保留双向 ±2 精确回声比对作第二层 |
| F3 | 双向底部锁定 | JS + Controller | 前向：编辑器已到底（`LinesOnScreen` 判定）时预览直接 `scrollTo` 页面底部而非块对齐（此时抑制全部上报 600ms）；反向：JS 检测预览到底时上报带 `;b` 后缀，控制器把编辑器滚到文档尾 |
| F4 | 前向 1 基对齐 | Controller + 接口 | 前向调用点统一换算为 1 基源码行（与 `data-line`/回声簿记同语义）；`ScrollToElementWithLineNo`/`PreviewScrollAction` 接口同步扩展 |

**已实施**（本轮提交，含接口 `IWebbrowserControl`/`IViewerInterface`/IE11 控件联动改动）；注入 JS 经 `node --check` 校验通过。

验收预期：3+4+5 同开时编辑器滚动全程平滑无回拽、可顺利到底且预览同步到底；预览滚动到底编辑器同步到底；双向互拖无振荡。

---

## 3. 发版记录（v1.2.2，2026-09-09）

| 步骤 | 结果 |
|------|------|
| 提交 | 远端 `a9513a5`（git push 被沙箱 SIGTERM，经 API blob→tree→commit→ref 通道；内容与本地 `c5be899` 一致） |
| build.yml | ✅ 全绿（4 job，run 34360920328） |
| tag v1.2.2 | ✅ API 创建：tag 对象 `6d6b35c` → `a9513a5` |
| release.yml | ✅ 全绿（run 34361389019） |
| **Release v1.2.2** | ✅ https://github.com/dororo42/npp-markdown-rustcore/releases/tag/v1.2.2 （x64 3.47MB + x86 3.43MB zip） |

**复测清单**：①菜单 3+4+5 同开，编辑器滚动应全程平滑、可顺利到底、预览同步到底；②预览滚到底 → 编辑器到底；③word-wrap + 双向同开无往返漂移；④用 `-fixed.md` 副本确认 3 处 `---` 分隔线恢复为横线、代码块/标题正常。
